import { MouseWheelListnerDirective } from './mouse-wheel-listner.directive';

describe('MouseWheelListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseWheelListnerDirective();
    expect(directive).toBeTruthy();
  });
});
